# MDN LocalLibrary Tutorial in Docker

Node / Mongodb development environment powered by docker.

Requirements:

- Docker Desktop

To run the services:

- `docker-compose build`
- `docker-compose up`
- `docker-compose run --rm --service-ports node_dev_env`

To clean up:

- `docker-compose down`
